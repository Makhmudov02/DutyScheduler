{
  "name": "duty-scheduler",
  "version": "1.0.0",
  "private": true,
  "description": "Offline duty scheduler (embedded WebView)",
  "scripts": {
    "build": "echo 'Static app - nothing to build' && exit 0",
    "copy": "npx cap copy",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}